

# Make Frost Media Logo Much Bigger in Hero

## Change
In `src/pages/MetaAds.tsx` line 147, increase the logo from `h-16 md:h-20` to `h-28 md:h-40` (112px mobile, 160px desktop). This makes it unmissable when people land on the page.

**File:** `src/pages/MetaAds.tsx` — single line change on line 147.

